<% if(action && actionName) { %>import { <%= actionName %> } from "<%= action %>";
import { createReducer<% if(async) { %>Async<% } %> } from "redux-act<% if(async) { %>-async<% } %>";<% } %>

<% if(!async) { %>const reducer = createReducer({}, {});
reducer.on(<%= actionName %>, state => state);
export const <%= upperCamel %>Reducer = reducer;
<% } %><% if(async) { %>
export const <%= upperCamel %>Reducer = createReducerAsync(<%= actionName %>);
<% } %>